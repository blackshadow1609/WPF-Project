<Window x:Name="Notepad" x:Class="Notepad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Notepad"
        mc:Ignorable="d"
        Title="Notepad" Height="450" Width="800" Icon="/Notepad.png">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="_Файл">
                <MenuItem Header="_Новый" Click="NewFile_Click" InputGestureText="Ctrl+N"/>
                <MenuItem Header="_Открыть" Click="OpenFile_Click" InputGestureText="Ctrl+O"/>
                <MenuItem Header="_Сохранить" Click="SaveFile_Click" InputGestureText="Ctrl+S"/>
                <MenuItem Header="Сохранить _как..." Click="SaveAsFile_Click"/>
                <Separator/>
                <MenuItem Header="В_ыход" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="_Правка">
                <MenuItem Header="_Отменить" Click="Undo_Click" InputGestureText="Ctrl+Z"/>
                <Separator/>
                <MenuItem Header="_Вырезать" Click="Cut_Click" InputGestureText="Ctrl+X"/>
                <MenuItem Header="_Копировать" Click="Copy_Click" InputGestureText="Ctrl+C"/>
                <MenuItem Header="_Вставить" Click="Paste_Click" InputGestureText="Ctrl+V"/>
                <MenuItem Header="_Удалить" Click="Delete_Click" InputGestureText="Del"/>
                <Separator/>
                <MenuItem Header="Выделить _все" Click="SelectAll_Click" InputGestureText="Ctrl+A"/>
                <MenuItem Header="_Время и дата" Click="TimeDate_Click" InputGestureText="F5"/>
            </MenuItem>
            <MenuItem Header="_Формат">
                <MenuItem Header="_Перенос по словам" IsCheckable="True" Click="WordWrap_Click"/>
                <MenuItem Header="_Шрифт..." Click="Font_Click"/>
            </MenuItem>
            <MenuItem Header="_Вид">
                <MenuItem Header="_Строка состояния" IsCheckable="True" IsChecked="True" Click="StatusBar_Click"/>
            </MenuItem>
            <MenuItem Header="_Справка">
                <MenuItem Header="Вызов _справки" Click="Help_Click"/>
                <Separator/>
                <MenuItem Header="_О программе" Click="About_Click"/>
            </MenuItem>
        </Menu>

        <TextBox x:Name="MainTextBox" Grid.Row="1" 
                 AcceptsReturn="True" 
                 AcceptsTab="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="14"
                 SpellCheck.IsEnabled="True"/>

        <StatusBar x:Name="MainStatusBar" Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="StatusText">Готов</TextBlock>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="EncodingText">UTF-8</TextBlock>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="CursorPosition">Стр 1, Стлб 1</TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>